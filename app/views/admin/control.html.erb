<h1>Control Panel</h1>
<div class="dashboard">
  <div class="admin btn-toolbar">
    <div class="btn-group">
      <a class="admin dropdown-toggle" data-toggle="dropdown" href="#">
        <h3><%= @repository.name %> <strong class="repository caret"></strong></h3>
      </a>
      <ul class="dropdown-menu" role="menu">
        <% @repositories.each do |repository| %>
          <li><a href="#"><%= repository.name %></a></li>
        <% end %>
      </ul>
    </div>
  </div>
  <table class="table">
    <thead>
      <tr>
        <th>Metric</th>
        <th>Status</th>
        <th>Action</th>
      </tr>
    </thead>
    <tbody>
      <% @metrics.each do |metric| %>
        <tr>
          <td rowspan="2"><%= metric.to_s.titlecase %></td>
          <td><div class="admin control progress"><div class="bar"></div></div></td>
          <td rowspan="2"><button class="btn"><strong class="icon-tasks"></strong></button></td>
        </tr>
        <tr>
          <td><div class="admin control progress"><div class="bar"></div></div></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>

<%= yield :heading %>
<ol class="report repository breadcrumb">
    <%= repository_selector %>
    <%= report_metric_selector if current_page?('metric#show')  %>
    <li class="active">Report</li>
  </li>
</ol>
<%= yield :content_top %>
<div class="dashboard">
  <%= yield :content %>
</div>

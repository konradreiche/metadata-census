<%= yield :heading %>
<ol class="report repository breadcrumb">
    <%= repository_selector %>
    <%= analysis_metric_selector if current_page?(:controller => 'metrics', :action => 'show')  %>
    <li class="active">Report</li>
  </li>
</ol>
<%= yield :content_top %>
<div class="dashboard">
  <%= yield :content %>
</div>

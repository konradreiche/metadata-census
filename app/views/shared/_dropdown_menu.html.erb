<a data-toggle="dropdown" href="#"><%= link_text %></a>
<ul class="dropdown-menu" role="menu">
  <% entities.each do |entity| %>
    <% parameter[:id] = entity %>
    <li><%= link_to entity.name, self.send(path, parameter) %></li>
  <% end %>
</ul>
<strong class="caret"></strong>
